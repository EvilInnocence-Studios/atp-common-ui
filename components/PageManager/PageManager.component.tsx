import {PageManagerProps} from "./PageManager.d";
import styles from './PageManager.module.scss';

export const PageManagerComponent = ({}:PageManagerProps) =>
    <div>PageManager component goes here.</div>;
